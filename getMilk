function getMilk(money, costPerBottle) {
    console.log("buy " + calBottels(money, costPerBottle)+ "bottle of milk")
    return calCahnge(money,costPerBottle)
}
function calBottels(money, costPerBottle){
    var numberOfBottle = Math.floor(money / moneyPerBottle)
    return numberOfBottle
}
function calCahnge(money, costPerBottle) {
    var ch ange = money % costPerBottle
    return change
}
console.log("here is your "+ getMilk(5,1.5) + " change")
